spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 120.27.11.244:9123
    config:
      discovery:
        enabled: true
        service-id: nacos-config-server
  profiles:
    active: dev